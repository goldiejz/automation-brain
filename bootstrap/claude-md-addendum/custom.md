## Constraints

Project type is `custom`. No prescriptive constraints from the bootstrap template — define them yourself in this file once the project's purpose and stack are clear.

Universal Strategix discipline still applies (see `~/.claude/CLAUDE.md` and `~/vaults/StrategixMSPDocs/project-standard.md` if this is a Strategix project): tenancy, RBAC, currency-suffix discipline, audit columns, schema-first migrations. Adopt them by default; deviate only with an explicit decision logged in `tasks/lessons.md`.

## Architecture Conventions

[TODO: Define once project shape stabilizes. Replace this section with the conventions you actually follow — runtime, data layer, auth, route/compute split, audit, event emission. Be specific. No placeholders left at first commit of real code.]

## Anti-Patterns

[TODO: Capture anti-patterns as you encounter them. Mirror them out of `tasks/lessons.md` as durable rules once a correction has been seen twice. Don't pre-populate generic ones — they don't bind without a real incident behind them.]
