---
phase: 03-self-improving-self-heal
plan: 08
type: execute
wave: 6
depends_on: [03-07]
files_modified:
  - STRUCTURE.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: [REQ-AOS-08, REQ-AOS-09, REQ-AOS-10, REQ-AOS-11, REQ-AOS-12, REQ-AOS-13, REQ-AOS-14]
must_haves:
  truths:
    - "STRUCTURE.md gains a `## AOS Self-Improving Self-Heal Contract` section after the Phase 2 `## AOS Escalation Contract`"
    - "REQUIREMENTS.md gains REQ-AOS-08 through REQ-AOS-14, each pointing at the SUMMARY file that proves it"
    - "STATE.md frontmatter advances to `current_phase: Phase 3 (AOS: Self-Improving Self-Heal)`, `status: complete`, progress 19/20 → (19+8)/(20+8) = 27/28 (or whatever the actual plan totals are)"
    - "STATE.md body gains a `## Phase 3 — AOS: Self-Improving Self-Heal (complete)` block listing all 8 plans with one-line outcomes"
    - "Schema commitment is repeated verbatim: schema_version stays 1; outcome is patched in place; class:self_improve is a NEW class value (not a schema bump)"
  artifacts:
    - path: "STRUCTURE.md"
      provides: "AOS Self-Improving Self-Heal Contract section"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-AOS-08..14 rows"
    - path: ".planning/STATE.md"
      provides: "Phase 3 complete marker + plan list"
  key_links:
    - from: "STRUCTURE.md"
      to: "scripts/policy-learner.sh + scripts/lib/outcome-tagger.sh"
      via: "named decision functions and lib paths"
      pattern: "policy-learner|outcome-tagger"
---

<objective>
Close Phase 3 with documentation: STRUCTURE.md contract, REQUIREMENTS minted, STATE.md marker.

Purpose: Acceptance criterion #8 — "Existing Tier 1–8 still pass" requires no doc drift, and downstream phases (4, 5, 6, 7) need a documented contract to depend on. Mirrors the role 02-09 played for Phase 2.
Output: 4 documents updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-self-improving-self-heal/CONTEXT.md
@.planning/phases/02-autonomy-policy/02-09-SUMMARY.md
@STRUCTURE.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AOS Self-Improving Self-Heal Contract to STRUCTURE.md</name>
  <files>STRUCTURE.md</files>
  <read_first>
    - /Users/jongoldberg/vaults/automation-brain/STRUCTURE.md (find the existing "## AOS Escalation Contract" section; the new section appends after it)
    - /Users/jongoldberg/vaults/automation-brain/.planning/phases/02-autonomy-policy/02-09-SUMMARY.md (mirror the section style and depth)
  </read_first>
  <action>
    Append a new section AFTER "## AOS Escalation Contract":

    ```markdown
    ## AOS Self-Improving Self-Heal Contract (Phase 3)

    Phase 3 closes the policy feedback loop. The audit log written by
    `_policy_log` (Phase 2) is read by a learner that scores patterns and
    auto-promotes / auto-deprecates them.

    ### Components

    | Component | Path | Role |
    |---|---|---|
    | Outcome tagger | `scripts/lib/outcome-tagger.sh` | SINGLE writer for the `outcome` field; reads delivery logs + git history within a configurable window (default 10 min) and patches `outcome` via jq, idempotent |
    | Policy learner | `scripts/policy-learner.sh` | Aggregates tagged decisions into `(class, decision, dispatcher, complexity)` patterns; emits promote/deprecate candidates to `observability/policy-evolution-pending.jsonl` |
    | Auto-patcher | inside `policy-learner.sh::learner_apply_pending` | Applies pending candidates to `~/vaults/ark/policy.yml` under a mkdir-lock (macOS-safe; no flock dep), commits each patch to the vault git repo, emits `_policy_log self_improve PROMOTED|DEPRECATED` audit entry |
    | Digest writer | inside `policy-learner.sh::learner_write_digest` | Writes `~/vaults/ark/observability/policy-evolution.md` (promoted, deprecated, mediocre sections) |

    ### Thresholds

    - Promote: `count >= 5 AND success_rate >= 0.80`
    - Deprecate: `count >= 5 AND success_rate <= 0.20`
    - Leave alone: `0.20 < success_rate < 0.80` OR `count < 5`

    Hardcoded for Phase 3 — configurable via policy.yml is deferred until data justifies it (CONTEXT.md decision #4).

    ### Schema commitment (NO change from Phase 2)

    `schema_version` stays at `1`. Phase 3 patches `outcome` in place via jq keyed on `decision_id`. The `class` value `self_improve` is added (used by auto-patch audit entries) — this is a new value, not a schema migration.

    Audit values for `class:self_improve`:
    - `decision`: `PROMOTED` or `DEPRECATED`
    - `reason`: `rate_pct_<NN>_count_<N>`
    - `context`: `{class, decision, dispatcher, complexity, rate_pct, count}`
    - `correlation_id`: the FIRST decision_id from the input set (chains the learning back to evidence)

    ### True-blocker classes are NEVER auto-patched

    The 4 escalation classes from the Phase 2 contract (monthly-budget, architectural-ambiguity, destructive-op, repeated-failure) are filtered out in `learner_emit_promotions` / `learner_emit_deprecations` AND re-checked at apply time (defense in depth). Filter logic: `class == "escalation"` OR `(class == "budget" AND decision == "ESCALATE_MONTHLY_CAP")`.

    ### Run cadence

    - Post-phase: `scripts/ark-deliver.sh::run_phase` invokes the learner with a windowed `--since` after `update_state` (non-fatal; learner failure does not fail the phase)
    - Manual: `ark learn` (default: last 7 days), `ark learn --full`, `ark learn --since DATE`

    ### Concurrency safety

    `learner_apply_pending` acquires `$VAULT_PATH/.policy-yml.lock` via `mkdir`-lock (30 s timeout). Two simultaneous learner runs serialize through this lock; neither corrupts policy.yml.

    ### Verification

    Tier 9 in `scripts/ark-verify.sh` runs a synthetic-fixture pipeline test in an isolated tmp vault (mirrors Phase 2 NEW-W-1 isolation pattern) and asserts:
    - 5/80% pattern → promotion
    - 5/20% pattern → deprecation
    - 5/50% pattern → no-op
    - true-blocker classes → no-op
    - real vault `policy-decisions.jsonl` unchanged after the test (isolation guarantee)

    ### Cross-references

    - Decision functions: `scripts/policy-learner.sh::{learner_score_patterns, learner_emit_promotions, learner_emit_deprecations, learner_apply_pending, learner_write_digest, learner_run}`
    - Outcome inference: `scripts/lib/outcome-tagger.sh::{outcome_classify, outcome_tag_decision, outcome_tag_window}`
    - Tier 9 verify: `scripts/ark-verify.sh` (search "Tier 9")
    - Plan history: `.planning/phases/03-self-improving-self-heal/{03-01..03-08}-SUMMARY.md`
    ```

    Verify: `grep -q "AOS Self-Improving Self-Heal Contract" STRUCTURE.md` succeeds.
  </action>
  <verify>
    <automated>grep -q "AOS Self-Improving Self-Heal Contract" /Users/jongoldberg/vaults/automation-brain/STRUCTURE.md && grep -q "outcome-tagger.sh" /Users/jongoldberg/vaults/automation-brain/STRUCTURE.md && grep -q "5/80\|>= 0.80" /Users/jongoldberg/vaults/automation-brain/STRUCTURE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AOS Self-Improving Self-Heal Contract" STRUCTURE.md` returns 1
    - `grep -q "policy-learner.sh" STRUCTURE.md && grep -q "outcome-tagger.sh" STRUCTURE.md` succeeds
    - `grep -q "schema_version" STRUCTURE.md` succeeds (commitment repeated; no claim of bump)
    - `grep -E "self_improve|PROMOTED|DEPRECATED" STRUCTURE.md | wc -l` >= 3
    - The Phase 2 "AOS Escalation Contract" section is unchanged (`grep -c "AOS Escalation Contract" STRUCTURE.md` returns 1; no duplicate / no removal)
  </acceptance_criteria>
  <done>
    STRUCTURE.md documents the Phase 3 contract end-to-end with cross-refs to code.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mint REQ-AOS-08..14 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - /Users/jongoldberg/vaults/automation-brain/.planning/REQUIREMENTS.md (existing R-001..R-008 + REQ-AOS-01..07; mirror the row format exactly)
  </read_first>
  <action>
    Append 7 new rows after the existing REQ-AOS-07 row. Map each to the corresponding Phase 3 acceptance criterion in CONTEXT.md and to the SUMMARY file that proves it:

    | ID | Statement | Evidence pointer | Status |
    |---|---|---|---|
    | REQ-AOS-08 | `scripts/policy-learner.sh` exists, sourceable + executable, self-test passes | 03-02-SUMMARY.md, 03-03-SUMMARY.md, 03-04-SUMMARY.md | done |
    | REQ-AOS-09 | `scripts/lib/outcome-tagger.sh` reads logs + git history within configurable window, patches `outcome` via jq, idempotent | 03-01-SUMMARY.md | done |
    | REQ-AOS-10 | `~/vaults/ark/policy.yml` is auto-patched at 5/80% (promote) or 5/20% (deprecate); each patch is a vault git commit | 03-03-SUMMARY.md | done |
    | REQ-AOS-11 | Each auto-patch logs `_policy_log self_improve PROMOTED|DEPRECATED` with correlation_id linking the source decision_ids | 03-03-SUMMARY.md | done |
    | REQ-AOS-12 | `~/vaults/ark/observability/policy-evolution.md` digest is generated by `ark learn` and post-phase trigger | 03-04-SUMMARY.md, 03-05-SUMMARY.md | done |
    | REQ-AOS-13 | `ark learn` subcommand exists (`--full`, `--since DATE`, default last-7-days) AND ark-deliver invokes learner post-phase | 03-05-SUMMARY.md, 03-06-SUMMARY.md | done |
    | REQ-AOS-14 | Tier 9 verify suite asserts promote/deprecate/no-op contract on synthetic fixture; Tier 1–8 still pass | 03-07-SUMMARY.md | done |

    Existing rows R-001..R-008 and REQ-AOS-01..07 untouched.
  </action>
  <verify>
    <automated>grep -c "REQ-AOS-0[8-9]\|REQ-AOS-1[0-4]" /Users/jongoldberg/vaults/automation-brain/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "REQ-AOS-08\|REQ-AOS-09\|REQ-AOS-1[0-4]" .planning/REQUIREMENTS.md` returns 7
    - Each new row points to at least one `.planning/phases/03-self-improving-self-heal/03-NN-SUMMARY.md` evidence file
    - `grep -c "REQ-AOS-0[1-7]" .planning/REQUIREMENTS.md` still returns 7 (Phase 2 rows untouched)
    - All 7 new rows have status `done`
  </acceptance_criteria>
  <done>
    7 new requirements minted; Phase 2 + Phase 3 requirements coexist cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md to mark Phase 3 complete</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - /Users/jongoldberg/vaults/automation-brain/.planning/STATE.md (current frontmatter + body)
    - /Users/jongoldberg/vaults/automation-brain/.planning/phases/02-autonomy-policy/02-09-SUMMARY.md (mirror the Phase 2 close pattern)
  </read_first>
  <action>
    Frontmatter updates:
    - `current_phase: Phase 3 (AOS: Self-Improving Self-Heal)`
    - `status: complete`
    - `last_updated:` → ISO8601 now
    - `progress.total_phases: 3`
    - `progress.completed_phases: 2` (or 3 — match the existing convention; STATE.md currently shows completed_phases=1 with Phase 2 marked complete in body, so authoring intent is "phases-with-all-plans-shipped"; bump to 3 since Phase 3 is now also complete)
    - `progress.total_plans:` → previous total + 8 (8 new Phase 3 plans)
    - `progress.completed_plans:` → previous + 8
    - `progress.percent:` → recompute (or leave at 100% for completed_phases / total_phases × 100)

    Body updates:
    - Header line: `**Current Phase:** Phase 3 (AOS: Self-Improving Self-Heal)` and `**Status:** complete`
    - Replace `## Phase 3+ — Future` placeholder block with a detailed `## Phase 3 — AOS: Self-Improving Self-Heal (complete)` block listing all 8 plans with one-line outcomes:

    ```markdown
    ## Phase 3 — AOS: Self-Improving Self-Heal (complete)

    See `.planning/phases/03-self-improving-self-heal/`

    **Goal:** Audit log → outcome tagger → pattern learner → auto-patch policy.yml under file-lock + git commit + audit entry. Self-improving without losing the schema lock or the true-blocker contract.

    **Exit gate:** Tier 9 (synthetic-fixture pipeline) passes; Tier 1–8 still pass.

    | Plan | Outcome |
    |------|---------|
    | 03-01 | `scripts/lib/outcome-tagger.sh`: SINGLE writer for `outcome` field; idempotent jq-patch; window-configurable inference |
    | 03-02 | `scripts/policy-learner.sh`: pattern scoring by `(class, decision, dispatcher, complexity)`; 5/80%/20% thresholds; true-blocker filter |
    | 03-03 | `learner_apply_pending`: mkdir-lock + python3/PyYAML patch + vault git commit + `_policy_log self_improve PROMOTED|DEPRECATED` audit |
    | 03-04 | `learner_write_digest`: `~/vaults/ark/observability/policy-evolution.md` with promoted, deprecated, mediocre sections; idempotent |
    | 03-05 | `scripts/ark-deliver.sh::run_phase` post-phase trigger (after `update_state`, non-fatal, windowed `--since`) |
    | 03-06 | `ark learn` subcommand (default last-7-days, `--full`, `--since DATE`) |
    | 03-07 | Tier 9 verify suite (synthetic fixture, isolated tmp vault, 11+ checks; mirrors Phase 2 NEW-W-1 isolation) |
    | 03-08 | STRUCTURE.md AOS Self-Improving Self-Heal Contract; REQ-AOS-08..14 minted; STATE.md updated |
    ```

    - Add `## Phase 4+ — Future` pointer to bootstrap autonomy.

    Phase 1 and Phase 2 sections left as-is.
  </action>
  <verify>
    <automated>grep -q "Phase 3 (AOS: Self-Improving Self-Heal)" /Users/jongoldberg/vaults/automation-brain/.planning/STATE.md && grep -q "## Phase 3 — AOS: Self-Improving Self-Heal (complete)" /Users/jongoldberg/vaults/automation-brain/.planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Phase 3 (AOS: Self-Improving Self-Heal)" .planning/STATE.md` succeeds
    - `grep -q "## Phase 3 — AOS: Self-Improving Self-Heal (complete)" .planning/STATE.md` succeeds
    - `grep -E "current_phase: Phase 3" .planning/STATE.md` returns 1 line
    - `grep -E "status: complete" .planning/STATE.md | head -1` matches the frontmatter status line
    - All 8 plan rows (03-01..03-08) appear in the body table (`grep -c "| 03-0[1-8] |" .planning/STATE.md` returns 8)
    - Phase 1 and Phase 2 sections still present and unchanged in shape (`grep -c "^## Phase [12]" .planning/STATE.md` returns 2)
    - `progress.total_plans` and `progress.completed_plans` increased by 8 vs the pre-Phase-3 STATE
  </acceptance_criteria>
  <done>
    STATE.md is the canonical "Phase 3 complete" record; future planners can `cat .planning/STATE.md` and see the truth.
  </done>
</task>

</tasks>

<verification>
- All three docs updated; `grep`-based checks pass
- Tier 1–9 still pass after the doc commit (`bash scripts/ark-verify.sh` exits 0)
- No code files modified by this plan (docs only)
</verification>

<success_criteria>
Phase 3 is documented end-to-end: STRUCTURE.md (contract), REQUIREMENTS.md (audit trail), STATE.md (truth). Phase 4 planning can begin against a stable Phase 3 contract.
</success_criteria>

<output>
After completion, create `.planning/phases/03-self-improving-self-heal/03-08-SUMMARY.md` listing the 3 docs touched, the 7 new requirements, and confirming Tier 9 still passes.
</output>
