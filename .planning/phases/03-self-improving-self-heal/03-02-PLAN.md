---
phase: 03-self-improving-self-heal
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - scripts/policy-learner.sh
autonomous: true
requirements: [REQ-AOS-08]
must_haves:
  truths:
    - "policy-learner.sh exists, is sourceable, executable, and self-test passes"
    - "Patterns are scored as `(class, decision, dispatcher, complexity)` tuples"
    - "Promotion fires ONLY at count >= 5 AND success_rate >= 0.80"
    - "Deprecation fires ONLY at count >= 5 AND success_rate <= 0.20"
    - "Mediocre middle (20% < rate < 80%) is left alone"
    - "Learner is idempotent: re-running over same window produces same scoring output"
    - "Learner NEVER overrides the 4 true-blocker escalation classes (monthly-budget, architectural-ambiguity, destructive-op, repeated-failure)"
  artifacts:
    - path: "scripts/policy-learner.sh"
      provides: "pattern scoring + promotion/deprecation engine"
      exports: ["learner_score_patterns", "learner_emit_promotions", "learner_emit_deprecations", "learner_run"]
  key_links:
    - from: "policy-learner.sh"
      to: "scripts/lib/outcome-tagger.sh"
      via: "source + outcome_tag_window"
      pattern: 'source.*outcome-tagger\.sh'
    - from: "policy-learner.sh"
      to: "observability/policy-decisions.jsonl"
      via: "jq aggregation by (class, decision, dispatcher, complexity)"
      pattern: 'jq.*policy-decisions\.jsonl'
---

<objective>
Build the pattern scoring + promotion/deprecation engine.

Purpose: Read the audit log (now with outcomes tagged by 03-01), aggregate by `(class, decision, dispatcher, complexity)`, and emit promotion/deprecation candidates. This plan emits decisions only — actually patching policy.yml is plan 03-03 (separation of concerns: scoring is testable in isolation; auto-patch needs file-locks and a vault commit).
Output: scripts/policy-learner.sh with `learner_run [--full | --since DATE]`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-self-improving-self-heal/CONTEXT.md
@.planning/phases/03-self-improving-self-heal/03-01-SUMMARY.md
@scripts/lib/outcome-tagger.sh
@scripts/ark-policy.sh
@scripts/lib/policy-config.sh

<interfaces>
<!-- Inputs from 03-01: -->
- outcome_classify <decision_id> [project_dir] [window_minutes] → success|failure|ambiguous
- outcome_tag_decision <decision_id> [project_dir] [window_minutes]
- outcome_tag_window <since_iso8601> [project_dir] [window_minutes]

<!-- Audit log line: see 03-01 -->

<!-- True-blocker escalation classes (NEVER promote/deprecate these): -->
- monthly-budget
- architectural-ambiguity
- destructive-op
- repeated-failure
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement policy-learner.sh scoring + emission engine</name>
  <files>scripts/policy-learner.sh</files>
  <read_first>
    - /Users/jongoldberg/vaults/automation-brain/.planning/phases/03-self-improving-self-heal/CONTEXT.md (decision #4: 5/80% promote, 5/20% deprecate, leave 20–80% alone; tuple shape locked)
    - /Users/jongoldberg/vaults/automation-brain/scripts/lib/outcome-tagger.sh (sourced; never reimplement outcome tagging)
    - /Users/jongoldberg/vaults/automation-brain/scripts/ark-policy.sh lines 1-54 (config loading pattern; mirror it)
  </read_first>
  <behavior>
    - `learner_score_patterns [--since ISO8601 | --full]` reads policy-decisions.jsonl, calls outcome_tag_window first (so all eligible lines have outcomes), then aggregates via jq:
      ```
      jq -s '
        group_by([.class, .decision, (.context.dispatcher // "none"), (.context.complexity // "none")])
        | map({
            class: .[0].class,
            decision: .[0].decision,
            dispatcher: (.[0].context.dispatcher // "none"),
            complexity: (.[0].context.complexity // "none"),
            count: length,
            success_count: ([.[] | select(.outcome == "success")] | length),
            failure_count: ([.[] | select(.outcome == "failure")] | length),
            ambiguous_count: ([.[] | select(.outcome == "ambiguous")] | length),
            decision_ids: [.[] | .decision_id]
          })
      '
      ```
    - `learner_emit_promotions <scored_json>` filters: `count >= 5 AND (success_count / count) >= 0.80 AND class is NOT in true-blocker list`. Emits one JSON object per promotion candidate to stdout.
    - `learner_emit_deprecations <scored_json>` filters: `count >= 5 AND (success_count / count) <= 0.20 AND class is NOT in true-blocker list`. Emits one JSON object per deprecation candidate to stdout.
    - `learner_run [--full | --since DATE]` → orchestrator: tag → score → emit promotions + deprecations to a sidecar file at `$VAULT_PATH/observability/policy-evolution-pending.jsonl`. (03-03 reads this file and applies patches.)
    - True-blocker class list hardcoded: `monthly-budget architectural-ambiguity destructive-op repeated-failure`. These are filtered out BEFORE emission. **Note:** Phase 2 audit-log uses `class: "budget"` for budget-class decisions; the true-blocker check operates on the SEMANTIC class. Filter logic: skip if `(.class == "budget" and .decision == "ESCALATE_MONTHLY_CAP")` or `.class == "escalation"` (which covers architectural-ambiguity, destructive-op, repeated-failure when emitted as escalation events).
    - Idempotency: re-running over the same window produces the same scored output (verified in self-test by running twice and diffing). Sidecar file at `$VAULT_PATH/observability/policy-evolution-pending.jsonl` is OVERWRITTEN each run, not appended (so re-runs don't accumulate stale candidates).
    - Bash 3 compat (no `declare -A`).
    - Self-test (`bash scripts/policy-learner.sh test`): synthesizes a tmp policy-decisions.jsonl with 4 patterns:
      1. (dispatch_failure, SELF_HEAL, gemini, deep) × 6 with 5 success / 1 failure → expect PROMOTION
      2. (dispatch_failure, SELF_HEAL, codex, simple) × 6 with 1 success / 5 failure → expect DEPRECATION
      3. (dispatch_failure, SELF_HEAL, haiku, medium) × 6 with 3 success / 3 failure → expect NO emission (mediocre)
      4. (budget, ESCALATE_MONTHLY_CAP, none, none) × 6 with 6 success → expect NO emission (true-blocker class)
      Then run `learner_run --full`, parse the sidecar, assert exactly 1 promotion + 1 deprecation + 0 true-blocker entries.
  </behavior>
  <action>
    Create scripts/policy-learner.sh:

    ```
    #!/usr/bin/env bash
    # policy-learner.sh — Pattern scoring + promotion/deprecation engine.
    # Phase 3 (REQ-AOS-08). Reads observability/policy-decisions.jsonl,
    # aggregates by (class, decision, dispatcher, complexity), emits promote/deprecate
    # candidates to observability/policy-evolution-pending.jsonl for plan 03-03 to apply.
    #
    # Bash 3 compatible (macOS).

    set -uo pipefail   # NOTE: no -e — graceful error reporting; functions return non-zero explicitly.

    VAULT_PATH="${ARK_HOME:-$HOME/vaults/ark}"
    POLICY_LOG="${POLICY_LOG:-$VAULT_PATH/observability/policy-decisions.jsonl}"
    PENDING_FILE="${PENDING_FILE:-$VAULT_PATH/observability/policy-evolution-pending.jsonl}"

    # Source outcome tagger (REQUIRED; if missing, abort with clear error)
    _LRN_LIB_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" 2>/dev/null && pwd)/lib"
    if [[ -f "$_LRN_LIB_DIR/outcome-tagger.sh" ]]; then
      # shellcheck disable=SC1091
      source "$_LRN_LIB_DIR/outcome-tagger.sh"
    else
      echo "❌ policy-learner.sh requires scripts/lib/outcome-tagger.sh" >&2
      exit 1
    fi

    PROMOTE_MIN_COUNT=5
    PROMOTE_MIN_RATE=80    # percent
    DEPRECATE_MAX_RATE=20  # percent

    TRUE_BLOCKER_CLASSES="monthly-budget architectural-ambiguity destructive-op repeated-failure"

    learner_score_patterns() { ... }
    learner_emit_promotions() { ... }
    learner_emit_deprecations() { ... }
    learner_run() { ... }

    # CLI entry
    if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
      case "${1:-run}" in
        test) ... ;;
        run|--full) learner_run --full ;;
        --since) shift; learner_run --since "$1" ;;
        *) echo "Usage: $0 [test|run|--full|--since DATE]" >&2; exit 1 ;;
      esac
    fi
    ```

    Implementation notes:
    - True-blocker filter: skip emission if `class == "escalation"` OR (`class == "budget"` AND `decision == "ESCALATE_MONTHLY_CAP"`).
    - Threshold math in jq using integer percent to avoid float issues:
      ```
      | (.success_count * 100 / .count) as $rate
      | select(.count >= 5 and $rate >= 80)
      ```
    - Sidecar JSON line schema:
      ```
      {"action":"promote|deprecate","class":"...","decision":"...","dispatcher":"...","complexity":"...","count":N,"success_count":N,"rate_pct":NN,"decision_ids":[...]}
      ```
    - learner_run flow:
      1. Resolve `since` (full = "1970-01-01T00:00:00Z")
      2. Call `outcome_tag_window "$since"`
      3. Run learner_score_patterns
      4. Pipe to learner_emit_promotions + learner_emit_deprecations
      5. Write combined output to PENDING_FILE (overwrite, not append)
      6. Echo summary to stdout: "Scored N patterns: P promotions, D deprecations"

    Make executable: `chmod +x scripts/policy-learner.sh`.
  </action>
  <verify>
    <automated>bash /Users/jongoldberg/vaults/automation-brain/scripts/policy-learner.sh test</automated>
  </verify>
  <acceptance_criteria>
    - File exists at scripts/policy-learner.sh and is executable
    - `bash scripts/policy-learner.sh test` exits 0
    - Self-test asserts: 1 promotion (gemini/deep/SELF_HEAL), 1 deprecation (codex/simple/SELF_HEAL), 0 entries for the mediocre and true-blocker patterns
    - `bash scripts/policy-learner.sh test` run twice produces byte-identical pending sidecar (idempotency)
    - `grep -c "^learner_score_patterns\|^learner_emit_promotions\|^learner_emit_deprecations\|^learner_run" scripts/policy-learner.sh` returns 4
    - `grep -E "PROMOTE_MIN_COUNT=5|PROMOTE_MIN_RATE=80|DEPRECATE_MAX_RATE=20" scripts/policy-learner.sh` returns 3 lines (thresholds present and correct)
    - `grep -E "TRUE_BLOCKER_CLASSES.*monthly-budget.*architectural-ambiguity.*destructive-op.*repeated-failure" scripts/policy-learner.sh` succeeds
    - Sourceable: `bash -c 'source scripts/policy-learner.sh && type learner_run'` exits 0 (NOTE: must protect CLI dispatch with `if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then ... fi`)
  </acceptance_criteria>
  <done>
    policy-learner.sh scores patterns, emits filtered promote/deprecate candidates to the pending sidecar, never touches true-blocker classes, and is idempotent.
  </done>
</task>

</tasks>

<verification>
- bash scripts/policy-learner.sh test → exits 0
- bash scripts/lib/outcome-tagger.sh test → still passes
- bash scripts/ark-policy.sh test → still passes
- Pending sidecar file produced byte-identical on re-run with same input
</verification>

<success_criteria>
Learner emits exactly the right promote/deprecate candidates for the synthetic test fixture; 03-03 can consume the pending sidecar.
</success_criteria>

<output>
After completion, create `.planning/phases/03-self-improving-self-heal/03-02-SUMMARY.md` documenting the scoring algorithm, the true-blocker filter, and the pending sidecar format.
</output>
