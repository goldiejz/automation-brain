---
phase: 03-self-improving-self-heal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/outcome-tagger.sh
autonomous: true
requirements: [REQ-AOS-09]
must_haves:
  truths:
    - "outcome-tagger.sh is the SINGLE writer for the `outcome` field of policy-decisions.jsonl (mirrors Phase 2's _policy_log single-writer pattern)"
    - "Re-running the tagger over the same window produces an identical log file (idempotent)"
    - "Tagger reads delivery logs + git history within a configurable window (default 10 minutes) of each decision_id"
    - "Tagger never bumps schema_version; outcome is patched in place via jq keyed on decision_id"
  artifacts:
    - path: "scripts/lib/outcome-tagger.sh"
      provides: "outcome inference + jq in-place patcher"
      exports: ["outcome_tag_window", "outcome_tag_decision", "outcome_classify"]
    - path: "scripts/lib/outcome-tagger.sh"
      provides: "self-test harness (runs when invoked with `test`)"
  key_links:
    - from: "outcome-tagger.sh"
      to: "observability/policy-decisions.jsonl"
      via: "jq read + atomic temp-file rewrite"
      pattern: 'jq -c .* policy-decisions\.jsonl'
    - from: "outcome-tagger.sh"
      to: "<project>/.planning/delivery-logs/*.log + git log"
      via: "shell heuristic (success/failure/ambiguous classifier)"
      pattern: "delivery-logs|git log --since"
---

<objective>
Build the foundational outcome-tagging library that every other Phase 3 component depends on.

Purpose: Phase 2 ships `outcome: null` in every audit log line. Phase 3 must retroactively patch each line with `"success" | "failure" | "ambiguous"` so the learner can score patterns. The tagger is the SINGLE writer for the outcome field — mirrors Phase 2's `_policy_log` single-writer / single-schema pattern (the iter-3 NEW-B-2 fix). Implementing this as a library (not inline) lets the learner, weekly digest, and any future AOS subsystem use the same heuristic.
Output: scripts/lib/outcome-tagger.sh with a sourceable API + self-test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-self-improving-self-heal/CONTEXT.md
@.planning/phases/02-autonomy-policy/02-01-SUMMARY.md
@scripts/ark-policy.sh
@scripts/lib/policy-config.sh

<interfaces>
<!-- Phase 2 audit log schema (LOCKED at schema_version=1). DO NOT bump. -->

```json
{
  "ts": "2026-04-26T10:23:45Z",
  "schema_version": 1,
  "decision_id": "20260426T102345Z-a1b2c3d4e5f67890",
  "class": "budget|dispatch|zero_tasks|dispatch_failure|escalation|self_heal|self_improve",
  "decision": "PROCEED|...",
  "reason": "<token>",
  "context": {...} | null,
  "outcome": null,            // <- Phase 3 patches this in place
  "correlation_id": null
}
```

decision_id format: `<YYYYMMDDTHHMMSSZ>-<16-hex>` (64-bit entropy).

Audit log path: `${ARK_HOME:-$HOME/vaults/ark}/observability/policy-decisions.jsonl`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement outcome-tagger.sh library with sourceable API</name>
  <files>scripts/lib/outcome-tagger.sh</files>
  <read_first>
    - /Users/jongoldberg/vaults/automation-brain/scripts/lib/policy-config.sh (mirror its sourced-lib hygiene: NO `set -euo pipefail` at top level; Bash 3 compat; self-test only when run directly)
    - /Users/jongoldberg/vaults/automation-brain/scripts/ark-policy.sh lines 56-91 (the `_policy_log` writer; tagger must NOT duplicate this — it only reads & patches)
  </read_first>
  <behavior>
    - `outcome_classify <decision_id> <project_dir> <window_minutes>` → echoes one of `success | failure | ambiguous`. Pure inference, no file writes.
    - `outcome_tag_decision <decision_id> [project_dir] [window_minutes]` → calls outcome_classify, then patches that single line in policy-decisions.jsonl in place via jq. Idempotent: if outcome is already non-null AND classification matches, no-op (returns 0). If non-null AND classification differs, leave existing value (non-destructive) and return 0 with a stderr note.
    - `outcome_tag_window <since_iso8601> [project_dir] [window_minutes]` → iterates every line with ts >= since AND outcome == null, calls outcome_tag_decision per line.
    - Inference rules (per CONTEXT.md decision #2):
      * `success` if a subsequent `dispatch` or `class:escalation` line in the SAME phase/correlation chain is absent AND a git commit landed within the window OR a follow-up decision in the same correlation chain has decision != ESCALATE_*
      * `failure` if a subsequent `class:escalation` line with the same correlation_id appears, OR a `class:self_heal` line with decision REJECTED appears in the window for the same context
      * `ambiguous` otherwise (no follow-up signal within window)
    - Configurable: window minutes via `policy_config_get phase3.outcome_window_minutes 10` (sources policy-config.sh if available; default 10).
    - Patching mechanism: `jq -c 'if .decision_id == $id and .outcome == null then .outcome = $o else . end' --arg id "$decision_id" --arg o "$verdict" "$LOG" > "$LOG.tmp" && mv "$LOG.tmp" "$LOG"`
    - Atomic write: `jq` to `$LOG.tmp.$$`, then `mv -f` (single-syscall rename on same fs).
    - Bash 3 compatible: no `declare -A`, no `${var,,}`. Test on macOS default bash.
    - Self-test (`bash scripts/lib/outcome-tagger.sh test`): synthesizes a tmp policy-decisions.jsonl with 4 lines (success case, failure case, ambiguous case, already-tagged case) and asserts:
      1. classify returns expected verdict for each
      2. tag_decision patches outcome field correctly
      3. re-running tag_decision is a no-op (idempotent — file md5 unchanged)
      4. tag_window iterates only `outcome == null` lines
      5. schema_version is still `1` after patching (grep)
      6. Bash 3 compat assertion (head -200 of self file → no `declare -A` outside test block)
  </behavior>
  <action>
    Create scripts/lib/outcome-tagger.sh with this exact structure:

    ```
    #!/usr/bin/env bash
    # outcome-tagger.sh — SINGLE writer for the `outcome` field of policy-decisions.jsonl.
    # Sourced by scripts/policy-learner.sh and scripts/ark-deliver.sh post-phase trigger.
    # Phase 3 (REQ-AOS-09). Schema is LOCKED at schema_version=1; outcome is patched in place.
    #
    # Bash 3 compatible (macOS). NO `set -euo pipefail` at top — it's a sourced lib.

    VAULT_PATH="${ARK_HOME:-$HOME/vaults/ark}"
    POLICY_LOG="${POLICY_LOG:-$VAULT_PATH/observability/policy-decisions.jsonl}"

    # Optional cascading config
    _OT_LIB_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" 2>/dev/null && pwd)"
    if [[ -f "$_OT_LIB_DIR/policy-config.sh" ]]; then
      # shellcheck disable=SC1091
      source "$_OT_LIB_DIR/policy-config.sh"
    else
      policy_config_get() { echo "$2"; }
    fi

    # Public: outcome_classify <decision_id> [project_dir] [window_minutes]
    outcome_classify() { ... }

    # Public: outcome_tag_decision <decision_id> [project_dir] [window_minutes]
    outcome_tag_decision() { ... }

    # Public: outcome_tag_window <since_iso8601> [project_dir] [window_minutes]
    outcome_tag_window() { ... }

    # Self-test
    if [[ "${BASH_SOURCE[0]}" == "${0}" ]] && [[ "${1:-}" == "test" ]]; then ... fi
    ```

    Implementation notes:
    - jq filter: `'if .decision_id == $id and .outcome == null then .outcome = $o else . end'`
    - For atomic write: write to `${POLICY_LOG}.tmp.$$`, then `mv -f` (POSIX rename atomic on same fs).
    - Heuristic for `success`: `jq -r 'select(.correlation_id == $cid or .decision_id == $cid)'` returns only the original line, AND `git -C "$project_dir" log --since="$ts" --until="<ts+window>"` shows a commit, AND no `class:escalation` line within the same correlation chain.
    - For `failure`: any later line with `class == "escalation"` AND `correlation_id == $decision_id`.
    - For `ambiguous`: neither condition met.
    - Use `date -u -j -f` (BSD) with fallback to GNU `date -d` for portability — example shim:
      ```
      _ot_to_epoch() { date -u -j -f "%Y-%m-%dT%H:%M:%SZ" "$1" +%s 2>/dev/null || date -u -d "$1" +%s; }
      ```
    - All log writes via `printf` to stderr (informational); no logger lib dep.

    Make the file executable: `chmod +x scripts/lib/outcome-tagger.sh`.

    Honor user decision D2 (CONTEXT.md): "Implementation in scripts/lib/outcome-tagger.sh so other AOS subsystems can call the same tagger."
  </action>
  <verify>
    <automated>bash /Users/jongoldberg/vaults/automation-brain/scripts/lib/outcome-tagger.sh test</automated>
  </verify>
  <acceptance_criteria>
    - File exists at scripts/lib/outcome-tagger.sh and is executable (`test -x`)
    - `bash scripts/lib/outcome-tagger.sh test` exits 0
    - Self-test prints "ALL OUTCOME-TAGGER TESTS PASSED"
    - `grep -c "^outcome_classify\|^outcome_tag_decision\|^outcome_tag_window" scripts/lib/outcome-tagger.sh` returns 3
    - `grep -q "schema_version" scripts/lib/outcome-tagger.sh` succeeds AND `grep -q 'schema_version[[:space:]]*[:=][[:space:]]*[02-9]' scripts/lib/outcome-tagger.sh` FAILS (only schema_version=1 references)
    - `grep -c 'declare -A\|\${[a-zA-Z_]*,,}' scripts/lib/outcome-tagger.sh` returns 0 (Bash 3 compat)
    - Idempotency assertion in self-test: md5sum of log unchanged on second tag_decision call for same id
    - Sourceable from another script without errors: `bash -c 'source scripts/lib/outcome-tagger.sh && type outcome_classify outcome_tag_decision outcome_tag_window'` exits 0 and prints function for all 3
  </acceptance_criteria>
  <done>
    outcome-tagger.sh is sourceable, self-test passes, idempotent, and is the SINGLE writer for the outcome field. Downstream plans (03-02, 03-04, 03-05) can source it.
  </done>
</task>

</tasks>

<verification>
- bash scripts/lib/outcome-tagger.sh test → exits 0 with "ALL OUTCOME-TAGGER TESTS PASSED"
- bash scripts/ark-policy.sh test → still passes (Phase 2 lib unchanged; tagger only reads + patches outcome field)
- bash scripts/lib/policy-config.sh test → still passes (no changes; tagger sources it)
</verification>

<success_criteria>
outcome-tagger.sh exists, is executable, self-test passes, and provides the three public functions used by 03-02, 03-04, 03-05.
</success_criteria>

<output>
After completion, create `.planning/phases/03-self-improving-self-heal/03-01-SUMMARY.md` documenting the public API, the inference rules, and the idempotency guarantee.
</output>
