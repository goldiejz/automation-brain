---
phase: 02-autonomy-policy
plan: 07
type: execute
wave: 3
depends_on: [02-03, 02-04, 02-05, 02-06, 02-06b]
files_modified:
  - scripts/ark-deliver.sh
  - scripts/ark-team.sh
  - scripts/execute-phase.sh
  - scripts/ark-budget.sh
  - scripts/self-heal.sh
  - observability/observer/patterns.json
autonomous: true
requirements: [REQ-AOS-01, REQ-AOS-06]
must_haves:
  truths:
    - "`grep -rEn 'read -[pr]' scripts/ark-deliver.sh scripts/ark-team.sh scripts/execute-phase.sh scripts/ark-budget.sh scripts/self-heal.sh` returns zero matches except lines tagged `# AOS: intentional gate`"
    - "Observer pattern `manual-gate-hit` exists in observability/observer/patterns.json with regex catching `read -p`, `press any key`, `continue\\?`, `y/N` in delivery-path log files"
    - "Pattern severity is critical with lesson_after_n=1 and tail target includes `.planning/delivery-logs/*.log`"
    - "Any remaining `read -[pr]` calls in non-delivery scripts (e.g., ark-create.sh interactive setup) are explicitly documented as intentional"
  artifacts:
    - path: "observability/observer/patterns.json"
      provides: "manual-gate-hit detection pattern"
      contains: "manual-gate-hit"
  key_links:
    - from: "observability/observer/patterns.json"
      to: ".planning/delivery-logs/*.log"
      via: "tail target match"
      pattern: "delivery-logs"
---

<objective>
Final autonomy audit: grep every delivery-path script for surviving `read -p` / `read -r` calls and either remove them (route through policy) or confirm the script is non-delivery-path. Add observer pattern `manual-gate-hit` so any future regression is caught the moment it appears in a delivery log.

**Note:** The self-heal.sh layered-retry refactor (CONTEXT.md decision #4) is handled in plan 02-06b (Wave 2), NOT here. This plan only audits self-heal.sh for surviving `read -[pr]` lines and tags any intentional gates. If 02-06b has already shipped, self-heal.sh should be clean.
</objective>

<context>
@/Users/jongoldberg/vaults/automation-brain/.planning/phases/02-autonomy-policy/CONTEXT.md
@/Users/jongoldberg/vaults/automation-brain/.planning/phases/02-autonomy-policy/02-06b-PLAN.md
@/Users/jongoldberg/vaults/automation-brain/scripts/ark-deliver.sh
@/Users/jongoldberg/vaults/automation-brain/scripts/ark-team.sh
@/Users/jongoldberg/vaults/automation-brain/scripts/execute-phase.sh
@/Users/jongoldberg/vaults/automation-brain/scripts/ark-budget.sh
@/Users/jongoldberg/vaults/automation-brain/scripts/self-heal.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit delivery-path scripts and strip remaining interactive reads</name>
  <files>scripts/self-heal.sh, scripts/ark-deliver.sh, scripts/ark-team.sh, scripts/execute-phase.sh, scripts/ark-budget.sh</files>
  <read_first>
    - All five files in files_modified
    - .planning/phases/02-autonomy-policy/CONTEXT.md (the 4 escalation classes — anything that would be `read -p`-prompted MUST become an ark_escalate or a policy decision)
    - .planning/phases/02-autonomy-policy/02-06b-PLAN.md (self-heal.sh layered-retry contract, already implemented in Wave 2)
  </read_first>
  <action>
    Step 1. Run the audit:

    ```
    grep -rEn 'read -[pr]|read.*PROMPT|read.*"[YyNn]"' \
      /Users/jongoldberg/vaults/automation-brain/scripts/ark-deliver.sh \
      /Users/jongoldberg/vaults/automation-brain/scripts/ark-team.sh \
      /Users/jongoldberg/vaults/automation-brain/scripts/execute-phase.sh \
      /Users/jongoldberg/vaults/automation-brain/scripts/ark-budget.sh \
      /Users/jongoldberg/vaults/automation-brain/scripts/self-heal.sh
    ```

    Step 2. For each match (if any survive after Wave 2):
    - If it's a destructive op confirmation (e.g., `--confirm` for production) → leave it AND add comment `# AOS: intentional gate — destructive op confirmation, see CONTEXT.md class 3`
    - If it's a routine resource decision → replace with appropriate policy call:
      - Budget question → `policy_budget_decision` + `ark_escalate monthly-budget`
      - Dispatcher question → `policy_dispatcher_route`
      - "Should I proceed?" question → drop entirely (default: proceed) OR convert to `ark_escalate architectural-ambiguity` if genuinely undecidable
    - If it's a self-heal "retry?" question → invoke `self-heal.sh --retry <task_id> <prompt> <output>` (the 02-06b layered-retry entry point)

    Step 3. Final assertion: re-run the grep from Step 1; result MUST be zero matches except lines tagged `# AOS: intentional gate`.
  </action>
  <verify>
    <automated>! grep -rEn 'read -[pr]|read.*PROMPT' /Users/jongoldberg/vaults/automation-brain/scripts/ark-deliver.sh /Users/jongoldberg/vaults/automation-brain/scripts/ark-team.sh /Users/jongoldberg/vaults/automation-brain/scripts/execute-phase.sh /Users/jongoldberg/vaults/automation-brain/scripts/ark-budget.sh /Users/jongoldberg/vaults/automation-brain/scripts/self-heal.sh | grep -v 'AOS: intentional gate'</automated>
  </verify>
  <done>
    - The audit grep returns zero matches except lines tagged `# AOS: intentional gate`
    - bash -n passes for every modified file
    - self-heal.sh layered-retry contract is honored (verified by 02-06b SUMMARY, not re-implemented here)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add observer pattern manual-gate-hit</name>
  <files>observability/observer/patterns.json</files>
  <behavior>
    - File observability/observer/patterns.json contains a JSON object with a top-level `patterns` array (or merges into existing schema — read first)
    - One entry has id="manual-gate-hit", severity="critical", lesson_after_n=1, regex catching `(read -[pr]|press any key|continue\?|y/N)` (case-insensitive), description mentioning "autonomy regression"
    - tail_targets includes both `.planning/delivery-logs/*.log` AND `~/vaults/ark/observability/dispatch-logs/*.log` (if the latter convention exists)
    - JSON validates with `python3 -c "import json,sys; json.load(open(sys.argv[1]))" observability/observer/patterns.json`
  </behavior>
  <read_first>
    - observability/observer/patterns.json (existing schema — must respect existing fields and pattern entries; do NOT delete unrelated patterns)
    - .planning/phases/02-autonomy-policy/CONTEXT.md (constraint #6: observer must catch future regressions)
  </read_first>
  <action>
    Read the existing file to determine its exact schema. Then append (merging) a new pattern entry:

    ```json
    {
      "id": "manual-gate-hit",
      "severity": "critical",
      "regex": "(read[[:space:]]+-[pr]|press[[:space:]]+any[[:space:]]+key|continue\\?|\\(y/N\\))",
      "regex_flags": "i",
      "lesson_after_n": 1,
      "description": "An autonomous-path script hit an interactive prompt — autonomy regression. Phase 2 banned these in delivery-path scripts; any new occurrence must route through policy_* or ark_escalate.",
      "tail_targets": [
        ".planning/delivery-logs/*.log",
        "$ARK_HOME/observability/dispatch-logs/*.log"
      ],
      "created_phase": "02-autonomy-policy"
    }
    ```

    If the file does not yet have a `patterns` array, create the file with that wrapper. Preserve any existing patterns.
  </action>
  <verify>
    <automated>python3 -c "import json,sys; d=json.load(open('/Users/jongoldberg/vaults/automation-brain/observability/observer/patterns.json')); ids=[p.get('id') for p in d.get('patterns', d if isinstance(d,list) else [])]; assert 'manual-gate-hit' in ids, 'pattern missing'; print('OK')"</automated>
  </verify>
  <done>
    - File parses as JSON
    - id `manual-gate-hit` is present
    - Existing patterns (if any) are preserved
  </done>
</task>

</tasks>

<verification>
- Final audit: zero unintentional `read -[pr]` matches in delivery-path scripts (zero matches except lines tagged `# AOS: intentional gate`)
- Observer pattern present and JSON-valid
</verification>

<success_criteria>
- Future regression of any manual gate is caught by the observer (1-occurrence threshold)
- self-heal.sh runs autonomously per the layered policy (verified by 02-06b, audited here)
</success_criteria>

<output>
`.planning/phases/02-autonomy-policy/02-07-SUMMARY.md` listing: every line removed/converted (file:line → before/after), the final audit grep output (must be empty modulo intentional-gate tags), and the exact patterns.json delta.
</output>
</content>
