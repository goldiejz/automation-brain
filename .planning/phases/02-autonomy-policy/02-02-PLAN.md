---
phase: 02-autonomy-policy
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - scripts/ark-escalations.sh
  - scripts/ark
autonomous: true
requirements: [REQ-AOS-03]
must_haves:
  truths:
    - "Calling `ark_escalate <class> <title> <body>` appends a section to ~/vaults/ark/ESCALATIONS.md with timestamp, status:open, and a stable id"
    - "Running `ark escalations` lists all open escalations (id, class, title, age) and exits 0"
    - "Running `ark escalations --resolve <id>` flips status to resolved and timestamps the resolution"
    - "ESCALATIONS.md is created on first call (file does not exist beforehand) — no error if missing"
    - "Each escalation also writes one audit-log entry via _policy_log with class=escalation"
  artifacts:
    - path: "scripts/ark-escalations.sh"
      provides: "Queue writer (ark_escalate function) + CLI lister (--list, --resolve, --show)"
      contains: "ark_escalate"
    - path: "scripts/ark"
      provides: "Top-level CLI; new subcommand `escalations` dispatches to ark-escalations.sh"
      contains: "escalations)"
  key_links:
    - from: "scripts/ark"
      to: "scripts/ark-escalations.sh"
      via: "case branch in ark CLI"
      pattern: "ark-escalations\\.sh"
    - from: "scripts/ark-escalations.sh"
      to: "~/vaults/ark/ESCALATIONS.md"
      via: "append section with id"
      pattern: "ESCALATIONS\\.md"
---

<objective>
Create the async escalation queue. When policy decides a true blocker is hit, scripts call `ark_escalate <class> <title> <body>` which appends a section to `~/vaults/ark/ESCALATIONS.md` and the user reviews via `ark escalations`.

Per CONTEXT.md decision #3: "Phase 2 implements queue write only; consumption (Phase 7) is later." So this plan ships the writer + a list/resolve CLI — not full lifecycle automation.

Output: a sourceable function used by Wave 2/3 wiring plans + an `ark escalations` command.
</objective>

<execution_context>
- Bash 3 compatibility, single-quoted Python heredocs
- ESCALATIONS.md format must be human-readable AND machine-parseable (regex-grepable section headers)
</execution_context>

<context>
@/Users/jongoldberg/vaults/automation-brain/.planning/phases/02-autonomy-policy/CONTEXT.md
@/Users/jongoldberg/vaults/automation-brain/.planning/phases/02-autonomy-policy/02-01-PLAN.md
@/Users/jongoldberg/vaults/automation-brain/scripts/ark
@/Users/jongoldberg/vaults/automation-brain/scripts/ark-policy.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create scripts/ark-escalations.sh — writer + CLI</name>
  <files>scripts/ark-escalations.sh</files>
  <behavior>
    - Sourceable: defines `ark_escalate <class> <title> <body>` without side effects on source
    - Direct invocation modes:
      - `ark-escalations.sh --list` (default): prints open escalations, one per row, columns: id | class | age | title
      - `ark-escalations.sh --show <id>`: prints the full body of escalation <id>
      - `ark-escalations.sh --resolve <id> [note]`: flips status from open to resolved, appends note + resolution timestamp
      - `ark-escalations.sh --all`: lists open AND resolved
    - Each escalation has a stable id format: `ESC-YYYYMMDD-HHMMSS-<6char-rand>` (sortable, unique-enough)
    - Class is one of: `monthly-budget`, `architectural-ambiguity`, `destructive-op`, `repeated-failure` (the 4 from CONTEXT.md). Reject unknown classes with exit 1 and a clear message.
    - Calling `ark_escalate` writes a `_policy_log "escalation" "<class>" ...` line via the audit logger if `ark-policy.sh` is sourced; if not, skip logging silently (graceful degradation)
  </behavior>
  <read_first>
    - scripts/ark-policy.sh (the _policy_log audit writer)
    - scripts/ark-escalations.sh (this file may not exist yet — that's fine)
    - CONTEXT.md (the 4 escalation classes)
  </read_first>
  <action>
    File: `scripts/ark-escalations.sh`. Top: shebang + `set -uo pipefail` only when not sourced. Use the `${BASH_SOURCE[0]}` vs `$0` check to detect sourced vs invoked.

    ESCALATIONS.md format (locked):

    ```markdown
    # Ark Escalations Queue

    > Async queue of true blockers. User reviews on session start or `ark escalations`.
    > Phase 2 writer-only. Phase 7 will consume responses from this file.

    ## ESC-20260426-093015-a1b2c3 — monthly-budget — open
    **Created:** 2026-04-26T09:30:15Z
    **Class:** monthly-budget
    **Title:** Monthly cap reached at 96% — need user decision

    <body>

    ---
    ```

    Section delimiter is `\n---\n` between escalations. The header line MUST match regex `^## (ESC-[0-9]{8}-[0-9]{6}-[a-z0-9]{6}) — (\S+) — (open|resolved)` so listing/resolution can parse robustly.

    Resolve: re-write the section header changing `— open` to `— resolved`, append `**Resolved:** <ts>` line and optional note. Do this with a single Python heredoc (single-quoted, so $vars don't interpolate; pass values via `os.environ`).

    Listing: parse with the same Python heredoc; tabular output via `printf` after sorting newest-first by id.

    Random id suffix: `head -c 8 /dev/urandom | base64 | tr -dc 'a-z0-9' | cut -c1-6` — Bash 3 safe.
  </action>
  <verify>
    <automated>bash -n /Users/jongoldberg/vaults/automation-brain/scripts/ark-escalations.sh && bash -c 'set -uo pipefail; source /Users/jongoldberg/vaults/automation-brain/scripts/ark-policy.sh; source /Users/jongoldberg/vaults/automation-brain/scripts/ark-escalations.sh; ARK_HOME=/tmp/ark-esc-test-$$ mkdir -p "$ARK_HOME"; ARK_HOME=/tmp/ark-esc-test-$$ ark_escalate monthly-budget "Test title" "Test body" && grep -q "open" /tmp/ark-esc-test-$$/ESCALATIONS.md && echo OK; rm -rf /tmp/ark-esc-test-*'</automated>
  </verify>
  <done>
    - File exists with `ark_escalate` function defined
    - `bash -n` passes
    - Round-trip test: write → list → show → resolve → list-all all work against a tmp ARK_HOME
    - Unknown class rejected with exit 1
    - Section header regex matches the locked format
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire `ark escalations` subcommand into the ark CLI</name>
  <files>scripts/ark</files>
  <read_first>
    - scripts/ark (full file — find the case dispatch table)
    - scripts/ark-escalations.sh (the script just created)
  </read_first>
  <action>
    Add a new case branch dispatching `ark escalations [args...]` to `scripts/ark-escalations.sh "$@"`. Mirror the dispatch pattern of an existing similar subcommand (e.g., `ark budget`, `ark verify`).

    Add help-text line: `escalations           # List/show/resolve queued blockers (~/vaults/ark/ESCALATIONS.md)` to whatever help block the ark CLI uses.

    Do NOT add prompts or interactive reads — purely dispatch.
  </action>
  <verify>
    <automated>bash -n /Users/jongoldberg/vaults/automation-brain/scripts/ark && /Users/jongoldberg/vaults/automation-brain/scripts/ark help 2>&1 | grep -q escalations && echo OK</automated>
  </verify>
  <done>
    - `bash -n scripts/ark` passes
    - `ark help` (or equivalent) lists `escalations`
    - `ark escalations --list` returns exit 0 (empty list is fine — "No open escalations." is acceptable output)
  </done>
</task>

</tasks>

<verification>
- ESCALATIONS.md only created on first true blocker (not at script source/install time)
- Section header regex: `^## ESC-[0-9]{8}-[0-9]{6}-[a-z0-9]{6} — (monthly-budget|architectural-ambiguity|destructive-op|repeated-failure) — (open|resolved)$`
- `ark escalations` (no args) doesn't error when file is absent
</verification>

<success_criteria>
- `ark_escalate` callable from any sourced context (Wave 3 wiring plans use it)
- User-facing CLI (`ark escalations`) exists and is non-interactive
- Audit log records escalations as `class=escalation` entries
</success_criteria>

<output>
Create `.planning/phases/02-autonomy-policy/02-02-SUMMARY.md` with:
- The locked section-header format
- Example ESCALATIONS.md after one synthetic write
- Mapping: which Phase 2 wiring plans will call `ark_escalate` (02-03 monthly-budget, 02-06 repeated-failure)
</output>
