---
phase: 02-autonomy-policy
plan: 05
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - scripts/ark-deliver.sh
autonomous: true
requirements: [REQ-AOS-01, REQ-AOS-02]
must_haves:
  truths:
    - "Phases with 0 actionable tasks call policy_zero_tasks; SKIP_LOGGED branch marks the phase complete and continues — no user prompt"
    - "If phase dir exists but has no PLAN.md AND policy returns ESCALATE_AMBIGUOUS, ark_escalate is invoked (architectural-ambiguity class) and the phase is skipped without halting the pipeline"
    - "No `read -p` / `read -r` calls in ark-deliver.sh"
    - "All zero-task decisions are audit-logged (zero_tasks class)"
  artifacts:
    - path: "scripts/ark-deliver.sh"
      provides: "Policy-routed zero-task and phase-collision handling"
      contains: "policy_zero_tasks"
  key_links:
    - from: "scripts/ark-deliver.sh"
      to: "scripts/ark-policy.sh"
      via: "source + call policy_zero_tasks at the existing zero-task warn site"
      pattern: "policy_zero_tasks"
---

<objective>
Replace zero-task warns and the "phase dir exists but no PLAN.md" silent return with `policy_zero_tasks` calls in `ark-deliver.sh`. After this plan, the pipeline never halts on zero-task phases — it either skips with audit log or queues an escalation.
</objective>

<context>
@/Users/jongoldberg/vaults/automation-brain/.planning/phases/02-autonomy-policy/CONTEXT.md
@/Users/jongoldberg/vaults/automation-brain/.planning/phases/02-autonomy-policy/02-01-PLAN.md
@/Users/jongoldberg/vaults/automation-brain/scripts/ark-deliver.sh
@/Users/jongoldberg/vaults/automation-brain/scripts/ark-policy.sh

<interfaces>
ark-policy.sh: `policy_zero_tasks <phase_dir> <plan_count>` returns `SKIP_LOGGED | ESCALATE_AMBIGUOUS`.
ark-escalations.sh: `ark_escalate architectural-ambiguity <title> <body>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Source policy + route zero-task branches via _deliver_handle_zero_tasks helper</name>
  <files>scripts/ark-deliver.sh</files>
  <behavior>
    - Conditional source of ark-policy.sh and ark-escalations.sh near top of file
    - New helper `_deliver_handle_zero_tasks <phase_num> <phase_dir> <plan_count>` calls policy_zero_tasks; on SKIP_LOGGED updates STATE.md to "complete (no tasks)"; on ESCALATE_AMBIGUOUS calls ark_escalate and updates STATE.md to "escalated (no tasks)". Always returns 0 so the pipeline continues to the next phase.
    - All three current sites in run_phase that warn-and-return on zero tasks are replaced by a single call to the helper followed by `return 0`:
      1. The "phase dir exists but no *-PLAN.md files" branch
      2. The `total_tasks -eq 0` branch
      3. The legacy single-PLAN.md `task_count -eq 0` branch
    - bash -n passes; zero `read -p` or `read -r` lines in the file
  </behavior>
  <read_first>
    - scripts/ark-deliver.sh (the run_phase function and surrounding helpers — focus on the three zero-task sites)
    - scripts/ark-policy.sh (policy_zero_tasks contract)
    - scripts/ark-escalations.sh (ark_escalate signature)
  </read_first>
  <action>
    Step 1. Insert near top of file, after `VAULT_PATH=...`:

    ```
    if [[ -f "$VAULT_PATH/scripts/ark-policy.sh" ]]; then source "$VAULT_PATH/scripts/ark-policy.sh"; fi
    if [[ -f "$VAULT_PATH/scripts/ark-escalations.sh" ]]; then source "$VAULT_PATH/scripts/ark-escalations.sh"; fi
    ```

    Step 2. Add the helper just before `run_phase`:

    ```
    _deliver_handle_zero_tasks() {
      local _phase="$1" _phase_dir="$2" _plan_count="$3"
      local _decision="SKIP_LOGGED"
      if type policy_zero_tasks >/dev/null 2>&1; then
        _decision=$(policy_zero_tasks "$_phase_dir" "$_plan_count")
      fi
      case "$_decision" in
        ESCALATE_AMBIGUOUS)
          if type ark_escalate >/dev/null 2>&1; then
            ark_escalate architectural-ambiguity \
              "Phase $_phase: zero actionable tasks" \
              "phase_dir: $_phase_dir | plan_count: $_plan_count" >/dev/null
          fi
          update_state "$_phase" "escalated (no tasks)"
          ;;
        *)
          update_state "$_phase" "complete (no tasks)"
          ;;
      esac
      return 0
    }
    ```

    Step 3. In `run_phase`, replace each of the three zero-task warn-and-return sites with:

    ```
    _deliver_handle_zero_tasks "$phase_num" "$phase_dir" "$plan_count"
    return 0
    ```

    For site #1 (phase dir exists but no PLAN.md), pass `plan_count=0`. For #2 use `$plan_count`. For #3 (legacy), `$plan_count` if set else 1.
  </action>
  <verify>
    <automated>bash -n /Users/jongoldberg/vaults/automation-brain/scripts/ark-deliver.sh; rc=$?; reads=$(grep -cE 'read -[pr]' /Users/jongoldberg/vaults/automation-brain/scripts/ark-deliver.sh); pol=$(grep -c 'policy_zero_tasks' /Users/jongoldberg/vaults/automation-brain/scripts/ark-deliver.sh); [[ $rc -eq 0 && $reads -eq 0 && $pol -ge 1 ]] && echo OK</automated>
  </verify>
  <done>
    - bash -n passes
    - grep for `read -[pr]` returns 0
    - grep for `policy_zero_tasks` returns ≥ 1
    - grep for `_deliver_handle_zero_tasks` returns ≥ 4 (1 def + 3 call sites)
    - No legacy "auto-skipping" warn message remaining (`grep -c 'auto-skipping' scripts/ark-deliver.sh` returns 0, OR the only remaining occurrence is inside the helper's escalation body string)
  </done>
</task>

</tasks>

<verification>
- `grep -cE 'read -[pr]' scripts/ark-deliver.sh` returns 0
- `grep -c '_deliver_handle_zero_tasks' scripts/ark-deliver.sh` ≥ 4
- ark-deliver.sh sources ark-policy.sh
</verification>

<success_criteria>
- Pipeline does not halt on zero-task phases under any condition
- Audit log captures every skip/escalate decision
</success_criteria>

<output>
`.planning/phases/02-autonomy-policy/02-05-SUMMARY.md` recording: the helper, the three replaced sites with line numbers, and any synthetic test runs.
</output>
