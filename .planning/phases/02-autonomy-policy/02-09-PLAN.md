---
phase: 02-autonomy-policy
plan: 09
type: execute
wave: 5
depends_on: [02-08]
files_modified:
  - STRUCTURE.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: [REQ-AOS-07]
must_haves:
  truths:
    - "STRUCTURE.md contains an `## AOS Escalation Contract` section enumerating the 4 true-blocker classes (monthly-budget, architectural-ambiguity, destructive-op, repeated-failure) with examples and mapping to ark-policy.sh decision functions"
    - "STRUCTURE.md documents the policy-decisions.jsonl schema verbatim (schema_version=1) including ALL Phase-3-ready fields: decision_id, outcome, correlation_id"
    - "REQUIREMENTS.md gains rows REQ-AOS-01..REQ-AOS-07 with status `done` and evidence pointing at the relevant plan SUMMARY files"
    - "STATE.md updates Current Phase to 2 with status complete after Tier 8 passes"
    - "~/.claude/skills/ark/SKILL.md (if it exists) gains an `## AOS Posture` line declaring delivery autonomy is active"
  artifacts:
    - path: "STRUCTURE.md"
      provides: "AOS escalation contract + audit log schema documentation (with Phase-3-ready fields)"
      contains: "AOS Escalation Contract"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-AOS-01..07 entries"
      contains: "REQ-AOS-01"
    - path: ".planning/STATE.md"
      provides: "Updated Current Phase / Status"
      contains: "Phase 2"
  key_links:
    - from: "STRUCTURE.md"
      to: "scripts/ark-policy.sh"
      via: "documentation references the decision functions by name"
      pattern: "policy_budget_decision|policy_dispatcher_route|policy_zero_tasks|policy_dispatch_failure"
---

<objective>
Document the AOS escalation contract and audit log schema so future Claude sessions (and Phase 3's observer-learner) have a stable reference. Update REQUIREMENTS.md and STATE.md to reflect the shipped state. Update SKILL.md if present.

This plan ships ONLY after Tier 8 passes (Plan 02-08). Documentation reflects what is real, not what was planned.

**W-6 fix:** STRUCTURE.md MUST document the full audit-log schema including `decision_id`, `outcome`, and `correlation_id` (added in 02-01 to make the schema Phase-3-ready). These fields ship in Phase 2 even though Phase 3 is the consumer.
</objective>

<context>
@/Users/jongoldberg/vaults/automation-brain/.planning/phases/02-autonomy-policy/CONTEXT.md
@/Users/jongoldberg/vaults/automation-brain/.planning/phases/02-autonomy-policy/INDEX.md
@/Users/jongoldberg/vaults/automation-brain/.planning/phases/02-autonomy-policy/02-01-PLAN.md
@/Users/jongoldberg/vaults/automation-brain/.planning/STATE.md
@/Users/jongoldberg/vaults/automation-brain/.planning/REQUIREMENTS.md
@/Users/jongoldberg/vaults/automation-brain/.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AOS Escalation Contract section to STRUCTURE.md</name>
  <files>STRUCTURE.md</files>
  <read_first>
    - STRUCTURE.md (full — existing section ordering, heading conventions)
    - scripts/ark-policy.sh (locked function signatures + _policy_log schema after 02-01 ships)
    - scripts/ark-escalations.sh (locked id format and section delimiter)
    - .planning/phases/02-autonomy-policy/02-01-PLAN.md (the W-6 schema fields are defined there)
  </read_first>
  <action>
    Append (or insert in appropriate location near other architecture docs) the following section:

    ```markdown
    ## AOS Escalation Contract

    Phase 2 transition: Ark decides routine resource questions autonomously and escalates only true blockers via async queue. The contract below is locked — Phase 3+ observer-learners read these formats.

    ### True-blocker classes (only 4 reach the user)

    | Class | Trigger | Decision function |
    |-------|---------|-------------------|
    | monthly-budget | Monthly token cap reached (>= 95% of monthly_cap_tokens) | policy_budget_decision → ESCALATE_MONTHLY_CAP |
    | architectural-ambiguity | Phase has 0 actionable tasks AND policy has no preference | policy_zero_tasks → ESCALATE_AMBIGUOUS |
    | destructive-op | Force-push, drop data, production deploy | (no policy fn — script-local guard with --confirm) |
    | repeated-failure | Same task fails self-heal max times (default 3) | policy_dispatch_failure → ESCALATE_REPEATED |

    All other questions (BLACK-tier with monthly headroom, CLI quota exhaustion, dispatch routing, zero-task phases, single retries) are decided by the policy engine without prompting.

    ### Queue location

    `~/vaults/ark/ESCALATIONS.md` — created on first escalation. Section format (regex-stable):

    ```
    ## ESC-YYYYMMDD-HHMMSS-<6char-rand> — <class> — <open|resolved>
    **Created:** <ISO8601 UTC>
    **Class:** <class>
    **Title:** <one-line>

    <body>

    ---
    ```

    User reviews via `ark escalations` (list/show/resolve). Phase 7 will consume responses written back to the file.

    ### Audit log

    Every policy decision writes one JSON line to `~/vaults/ark/observability/policy-decisions.jsonl`. Schema (locked, schema_version=1):

    ```json
    {
      "ts": "<ISO8601 UTC>",
      "schema_version": 1,
      "decision_id": "<YYYYMMDDTHHMMSSZ>-<6-digit-random>",
      "class": "<budget|dispatch|zero_tasks|dispatch_failure|escalation|self_heal>",
      "decision": "<UPPER_SNAKE_CASE>",
      "reason": "<machine_readable_token_pattern>",
      "context": <json-object-or-null>,
      "outcome": null,
      "correlation_id": null
    }
    ```

    Field semantics:

    | Field | Phase 2 writer | Phase 3 reader |
    |-------|----------------|----------------|
    | `ts` | ISO8601 UTC, set by `_policy_log` | Read |
    | `schema_version` | Always `1` | Branch on this |
    | `decision_id` | Auto-generated unique ID per line. Format: `%Y%m%dT%H%M%SZ-NNNNNN` (compact ts + 6-digit random). Phase 3 patches `outcome` keyed on this ID. | Patch target |
    | `class` | One of the listed enum values | Filter by this |
    | `decision` | UPPER_SNAKE_CASE return value of the decision fn | Read |
    | `reason` | Machine-readable token pattern (no free text; greppable) | Cluster patterns by this |
    | `context` | Decision-specific JSON object (input args, derived values), or `null` | Feature input for learner |
    | `outcome` | ALWAYS `null` in Phase 2 | Phase 3 patches this to `"success"` / `"failure"` / `"ambiguous"` after observing the result |
    | `correlation_id` | `null` by default; caller may pass a prior `decision_id` to link causally-related decisions | Reconstruct decision chains |

    Decision values per class:
    - budget: AUTO_RESET | PROCEED | ESCALATE_MONTHLY_CAP
    - dispatch: codex | gemini | haiku-api | claude-session | regex-fallback
    - zero_tasks: SKIP_LOGGED | ESCALATE_AMBIGUOUS
    - dispatch_failure: RETRY_NEXT_TIER | SELF_HEAL | ESCALATE_REPEATED
    - escalation: \<class-name\>
    - self_heal: RETRY_1_ENRICHED | RETRY_2_MODEL_ESCALATE | RETRY_3_ESCALATE_QUEUE

    **Why these fields ship in Phase 2:** Phase 3 (self-improving self-heal) needs to (a) tag each decision with its eventual outcome and (b) reconstruct chains of related decisions. Doing this retroactively requires a stable per-line ID and a chain-link field. Adding them after Phase 2 ships would either break schema_version=1 or require Phase 3 to do log-rewrites without keys. Shipping them now (with `null` defaults) means Phase 2 writes complete records that Phase 3 can patch in place.

    ### Cascading config

    Resolution order (highest wins):
    1. Env var (`ARK_<UPPER_SNAKE>`)
    2. `<project>/.planning/policy.yml`
    3. `~/vaults/ark/policy.yml`
    4. Built-in defaults in `scripts/ark-policy.sh`

    Keys consumed today: `budget.monthly_escalate_pct`, `self_heal.max_retries`, `budget.phase_cap_default`, `budget.monthly_cap_default`. Keys may be added; existing keys MUST NOT change semantics.

    ### Layered self-heal contract (CONTEXT.md decision #4)

    `scripts/self-heal.sh --retry <task_id> <prompt> <output>` implements three layers, gated by file-backed retry count at `<phase_dir>/self-heal-retries-<task_id>.txt`:

    1. **Retry 1 (count==0 → 1):** enriched prompt — appends lessons.md tail + last error blob to the prompt, dispatches via the same dispatcher
    2. **Retry 2 (count==1 → 2):** model escalate — `policy_dispatcher_route deep` picks next-tier dispatcher, dispatches with the original prompt
    3. **Retry 3 (count==2 → 3):** queue escalation — `ark_escalate repeated-failure`, exits 2

    ark-team.sh's `dispatch_role` runs an in-process retry loop (count_file reset per `ark team` invocation, 4 dispatches max per role). The two retry surfaces do NOT overlap: ark-team handles single-role dispatcher-swap retries within an invocation; self-heal.sh handles task-level cross-invocation retries.

    ### Observer pattern

    `manual-gate-hit` (severity: critical, lesson_after_n: 1) — fires on any future occurrence of `read -p`, `read -r`, `press any key`, `continue?`, `(y/N)` in delivery-path log files. Existing intentional gates are tagged `# AOS: intentional gate` in source.
    ```

    Adjust heading depth to match the existing STRUCTURE.md layout. If STRUCTURE.md doesn't yet exist at the vault root, create it with a minimal preamble.
  </action>
  <verify>
    <automated>grep -q 'AOS Escalation Contract' /Users/jongoldberg/vaults/automation-brain/STRUCTURE.md && grep -q 'schema_version' /Users/jongoldberg/vaults/automation-brain/STRUCTURE.md && grep -q 'decision_id' /Users/jongoldberg/vaults/automation-brain/STRUCTURE.md && grep -q 'correlation_id' /Users/jongoldberg/vaults/automation-brain/STRUCTURE.md && grep -qE 'monthly-budget|architectural-ambiguity|destructive-op|repeated-failure' /Users/jongoldberg/vaults/automation-brain/STRUCTURE.md && echo OK</automated>
  </verify>
  <done>
    - Section present
    - All 4 escalation classes named
    - Audit schema documented with schema_version=1 AND decision_id AND outcome AND correlation_id
    - Cascading config order documented
    - Layered self-heal contract documented (3 layers + ark-team disambiguation)
    - manual-gate-hit observer pattern referenced
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md (mint REQ-AOS-01..07) and STATE.md (mark Phase 2 complete)</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current 8 rows R-001..R-008)
    - .planning/STATE.md
    - All 02-NN-SUMMARY.md files written by previous plans (evidence)
  </read_first>
  <action>
    REQUIREMENTS.md — append rows:

    | ID | Requirement | Status | Evidence |
    |----|-------------|--------|----------|
    | REQ-AOS-01 | `ark deliver` runs to completion under simulated quota+budget exhaustion with zero stdin reads | done | .planning/phases/02-autonomy-policy/02-08-SUMMARY.md |
    | REQ-AOS-02 | scripts/ark-policy.sh + cascading config; ark-deliver/ark-team/execute-phase/ark-budget delegate routing | done | 02-01..02-06b SUMMARYs |
    | REQ-AOS-03 | ESCALATIONS.md queue + `ark escalations` command | done | 02-02-SUMMARY.md |
    | REQ-AOS-04 | policy-decisions.jsonl audit log (schema_version=1, includes decision_id/outcome/correlation_id) | done | 02-01-SUMMARY.md, STRUCTURE.md |
    | REQ-AOS-05 | Tier 8 verify; Tier 1–7 still pass | done | 02-08-SUMMARY.md |
    | REQ-AOS-06 | Observer manual-gate-hit pattern | done | 02-07-SUMMARY.md |
    | REQ-AOS-07 | STRUCTURE.md AOS escalation contract documented | done | this plan SUMMARY |

    STATE.md — update:
    - `**Last updated:**` to current UTC timestamp
    - `**Current Phase:**` → `Phase 2 (AOS: Delivery Autonomy)`
    - `**Status:**` → `complete`
    - Add `## Phase 2 — AOS: Delivery Autonomy (complete)` block listing the 10 plans (02-01..02-09 + 02-06b) with brief outcome
    - Phase 1 section: leave as-is (it's mid-flight per current STATE.md; this plan does not change Phase 1 status)

    Per CLAUDE.md truth hierarchy: STATE.md is primary implementation truth. Update only after Tier 8 passes.
  </action>
  <verify>
    <automated>grep -c 'REQ-AOS-0[1-7]' /Users/jongoldberg/vaults/automation-brain/.planning/REQUIREMENTS.md | grep -qE '^[7-9]|^[1-9][0-9]+$' && grep -q 'Phase 2' /Users/jongoldberg/vaults/automation-brain/.planning/STATE.md && echo OK</automated>
  </verify>
  <done>
    - All 7 REQ-AOS-NN rows present in REQUIREMENTS.md
    - STATE.md reflects Phase 2 complete with timestamp
    - STATE.md lists 10 plans (including 02-06b)
    - Evidence column points at real SUMMARY files
  </done>
</task>

<task type="auto">
  <name>Task 3: Update ~/.claude/skills/ark/SKILL.md AOS posture (if file exists)</name>
  <files>~/.claude/skills/ark/SKILL.md</files>
  <read_first>
    - ~/.claude/skills/ark/SKILL.md (if it exists — check first)
  </read_first>
  <action>
    If `~/.claude/skills/ark/SKILL.md` exists, add (or update) a top-level section:

    ```markdown
    ## AOS Posture (since Phase 2)

    Ark is in **AOS delivery autonomy** mode. Routine resource decisions (budget BLACK with headroom, CLI quota exhaustion, dispatch routing, zero-task phases, single retries) are decided by `scripts/ark-policy.sh`. Only 4 true-blocker classes reach the user via `~/vaults/ark/ESCALATIONS.md`:

    1. monthly-budget (>=95% monthly cap)
    2. architectural-ambiguity (no policy preference)
    3. destructive-op (force-push, prod deploy, drop data)
    4. repeated-failure (3+ retries on same task)

    Anything else: policy decides, decision is audit-logged to `~/vaults/ark/observability/policy-decisions.jsonl` (schema_version=1, includes decision_id for Phase-3 outcome patching), work proceeds. See STRUCTURE.md `AOS Escalation Contract` for full schema.
    ```

    If the file does not exist: log a note in the SUMMARY and skip — do not create it speculatively.
  </action>
  <verify>
    <automated>if [[ -f ~/.claude/skills/ark/SKILL.md ]]; then grep -q 'AOS Posture' ~/.claude/skills/ark/SKILL.md && echo OK; else echo "SKILL.md absent — skipped per plan"; fi</automated>
  </verify>
  <done>
    - If SKILL.md exists: AOS Posture section present
    - If absent: skip is documented in 02-09-SUMMARY.md (not silently ignored)
  </done>
</task>

</tasks>

<verification>
- STRUCTURE.md, REQUIREMENTS.md, STATE.md all updated
- `bash scripts/ark-verify.sh` (full) still exits 0
- `cat .planning/REQUIREMENTS.md | grep -c REQ-AOS` returns 7
- STRUCTURE.md schema block contains decision_id, outcome, correlation_id
</verification>

<success_criteria>
- Phase 2 documented end-to-end
- Phase 3 has a stable schema to consume (with decision_id/outcome/correlation_id pre-shipped)
- STATE.md is the single source of truth for "Phase 2 = complete"
</success_criteria>

<output>
`.planning/phases/02-autonomy-policy/02-09-SUMMARY.md` recording the docs delta and any deferred items (e.g., SKILL.md absent).
</output>
</content>
