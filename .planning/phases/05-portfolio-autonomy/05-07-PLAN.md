---
phase: 05-portfolio-autonomy
plan: 07
type: execute
wave: 5
depends_on: ["05-06"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - STRUCTURE.md
  - SKILL.md
autonomous: true
requirements:
  - REQ-AOS-23
  - REQ-AOS-24
  - REQ-AOS-25
  - REQ-AOS-26
  - REQ-AOS-27
  - REQ-AOS-28
  - REQ-AOS-29
  - REQ-AOS-30
must_haves:
  truths:
    - "REQ-AOS-23..30 minted in .planning/REQUIREMENTS.md with statement + plan(s) + owning script"
    - ".planning/STATE.md shows Phase 5 complete with exit-gate evidence (Tier 11 12+/12+; Tier 7-10 retained)"
    - ".planning/ROADMAP.md Phase 5 checkboxes all checked"
    - "STRUCTURE.md has an 'AOS Portfolio Autonomy Contract' section with the 4 decision classes, the priority formula, and the cool-down rule"
    - "SKILL.md (best-effort) updated with Phase 5 capabilities"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-AOS-23..30 rows"
      contains: "REQ-AOS-23|REQ-AOS-30"
    - path: ".planning/STATE.md"
      provides: "Phase 5 complete entry"
      contains: "Phase 5.*complete|Portfolio Autonomy"
    - path: "STRUCTURE.md"
      provides: "AOS Portfolio Autonomy Contract"
      contains: "Portfolio Autonomy|priority formula|DEFERRED_BUDGET"
  key_links:
    - from: ".planning/STATE.md"
      to: "scripts/ark-portfolio-decide.sh"
      via: "Phase 5 plan-table row referencing the script"
      pattern: "ark-portfolio-decide.sh"
---

<objective>
Close Phase 5 documentation. Mint REQ-AOS-23..30. Update STATE.md to mark Phase 5
complete with exit-gate evidence. Tick ROADMAP.md checkboxes. Add the AOS Portfolio
Autonomy Contract to STRUCTURE.md. Best-effort SKILL.md refresh.

Purpose: Documentation truth-up — REQ IDs minted, STATE marks Phase 5 closed,
contracts captured for future phases (Phase 6 cross-customer learning will read
these portfolio audits).

Output: Doc-only changes; no code.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-portfolio-autonomy/CONTEXT.md
@.planning/phases/05-portfolio-autonomy/INDEX.md
@.planning/phases/05-portfolio-autonomy/05-01-PLAN.md
@.planning/phases/05-portfolio-autonomy/05-02-PLAN.md
@.planning/phases/05-portfolio-autonomy/05-03-PLAN.md
@.planning/phases/05-portfolio-autonomy/05-04-PLAN.md
@.planning/phases/05-portfolio-autonomy/05-05-PLAN.md
@.planning/phases/05-portfolio-autonomy/05-06-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mint REQ-AOS-23..30 in .planning/REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full) — match the existing row format used for REQ-AOS-15..22 (Phase 4). Mirror columns + ordering.
  </read_first>
  <action>
    Append 8 rows in the exact format used by Phase 4 rows. Use the table below
    verbatim (columns: ID | Statement | Plan(s) | Owning script):

    | REQ-AOS-23 | scripts/ark-portfolio-decide.sh exists; sourceable; self-test passes | 05-01 | scripts/ark-portfolio-decide.sh |
    | REQ-AOS-24 | `ark deliver` (no args) picks highest-priority project; zero prompts | 05-05 | scripts/ark-deliver.sh |
    | REQ-AOS-25 | Decision audit-logged via `_policy_log "portfolio" "SELECTED" ...` with full priority breakdown | 05-04, 05-05 | scripts/ark-portfolio-decide.sh |
    | REQ-AOS-26 | Per-customer monthly budget caps honored; over-cap customers' projects DEFERRED | 05-02 | scripts/ark-portfolio-decide.sh |
    | REQ-AOS-27 | CEO directive override: explicit priority in programme.md beats heuristic | 05-03 | scripts/ark-portfolio-decide.sh |
    | REQ-AOS-28 | Existing `ark deliver --phase N` (single project, current dir) unchanged (backward compat) | 05-05, 05-06 | scripts/ark-deliver.sh |
    | REQ-AOS-29 | Tier 11 verify: synthetic 3-project / 2-customer fixture with varying priority signals | 05-06 | scripts/ark-verify.sh |
    | REQ-AOS-30 | Existing Tier 1–10 still pass (no regression) | 05-06 | scripts/ark-verify.sh |

    If REQUIREMENTS.md uses a different format (heading list, frontmatter, etc.),
    adapt to that exact format for these 8 IDs.
  </action>
  <verify>
    <automated>grep -c 'REQ-AOS-2[3-9]\|REQ-AOS-30' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - 8 new requirement rows present
    - Each REQ-AOS-23..30 appears exactly once
    - Format matches existing Phase 4 row format
  </acceptance_criteria>
  <done>Requirements minted.</done>
</task>

<task type="auto">
  <name>Task 2: Update .planning/STATE.md — Phase 5 complete</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (full) — current frontmatter shows `current_phase: "Phase 4"`, `completed_phases: 4`, `total_plans: 34`, `completed_plans: 33`. We update for Phase 5 close.
    - .planning/phases/05-portfolio-autonomy/05-06-SUMMARY.md (Tier 7-11 actual counts) — read to extract real numbers, not placeholders.
  </read_first>
  <action>
    Edit `.planning/STATE.md`:

    1. **Frontmatter:**
       - `current_phase: "Phase 5 (AOS: Portfolio Autonomy)"`
       - `status: complete`
       - `last_updated: "<now ISO-8601 UTC>"` (use `date -u +%Y-%m-%dT%H:%M:%SZ`)
       - `progress.completed_phases: 5`
       - `progress.total_plans: 41` (34 + 7 Phase 5 plans)
       - `progress.completed_plans: 41`
       - `progress.percent: 71` (5/7 ≈ 71%)

    2. **Body:** Update top header line to match: `**Current Phase:** Phase 5 (AOS: Portfolio Autonomy)` and `**Status:** complete` and `**Last updated:** <ISO>`.

    3. **Append a new section** (mirroring the Phase 4 section format):
       ```markdown
       ## Phase 5 — AOS: Portfolio Autonomy (complete)

       See `.planning/phases/05-portfolio-autonomy/`

       **Goal:** `ark deliver` (no args) picks the highest-leverage project from
       the portfolio and runs its next phase, audit-logged, with cross-project
       budget routing and 24h cool-down.

       **Exit gate:** Tier 11 <PASS>/<TOTAL> + Tier 7/8/9/10 retained — confirmed
       `bash scripts/ark-verify.sh --tier 7` 14/14, `--tier 8` 25/25, `--tier 9`
       20/20, `--tier 10` 22/22, `--tier 11` <PASS>/<TOTAL>.

       | Plan | Outcome |
       |------|---------|
       | 05-01 | `scripts/ark-portfolio-decide.sh` foundation: discovery + scoring + winner; sentinel sections; bash-3 self-test |
       | 05-02 | `_portfolio_budget_headroom`: per-customer monthly cap reader via existing cascading config; 0 headroom signals DEFERRED_BUDGET |
       | 05-03 | `_portfolio_ceo_priority`: programme.md `## Next Priority` parser; +5 weight when match |
       | 05-04 | `_portfolio_recently_deferred` + full `portfolio_decide`: 4 decision classes (SELECTED/DEFERRED_BUDGET/DEFERRED_HEALTHY/NO_CANDIDATE_AVAILABLE) audit-logged via single-writer `_policy_log`; 24h cool-down via sqlite SELECT against `class:portfolio` history |
       | 05-05 | `scripts/ark-deliver.sh` no-args portfolio routing branch; `scripts/ark` help text; backward compat for `--phase N` and in-project invocation |
       | 05-06 | Tier 11 verify suite (synthetic 3-project / 2-customer fixture, 12+ checks; isolated mktemp vault; real DB md5 invariant) |
       | 05-07 | REQ-AOS-23..30 minted; STATE.md Phase 5 close; ROADMAP.md checkboxes; STRUCTURE.md AOS Portfolio Autonomy Contract; SKILL.md best-effort |
       ```
       Replace `<PASS>` and `<TOTAL>` with the actual Tier 11 numbers from
       05-06-SUMMARY.md.

    4. Update the trailing "Phase 5+ — Future" section to start at Phase 6:
       Replace `Next per .planning/ROADMAP.md: **Phase 5 — Portfolio Autonomy** ...`
       with: `Next per .planning/ROADMAP.md: **Phase 6 — Cross-Customer Learning Autonomy** (lessons learned in one customer auto-promote to universal when the same pattern recurs in 2+ customers; Tier 12 verify).`

    Atomic write: write to .planning/STATE.md.tmp then mv.
  </action>
  <verify>
    <automated>grep -q 'Phase 5 — AOS: Portfolio Autonomy (complete)' .planning/STATE.md && grep -q 'completed_phases: 5' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter `completed_phases: 5`, `total_plans: 41`, `completed_plans: 41`
    - Body section "Phase 5 — AOS: Portfolio Autonomy (complete)" present with 7-row plan table
    - Tier numbers from 05-06-SUMMARY substituted (no `<PASS>`/`<TOTAL>` placeholders left)
  </acceptance_criteria>
  <done>STATE.md reflects Phase 5 complete.</done>
</task>

<task type="auto">
  <name>Task 3: Tick .planning/ROADMAP.md Phase 5 checkboxes</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 68-78 (the Phase 5 checklist).
  </read_first>
  <action>
    Convert each `- [ ]` under "### Phase 5 — AOS: Portfolio Autonomy" to `- [x]`.
    Add a final line at end of the section: `**Status:** ✅ Complete — see .planning/phases/05-portfolio-autonomy/`.

    Do NOT touch Phase 6 / 7 / 8 sections.
  </action>
  <verify>
    <automated>awk '/### Phase 5/,/### Phase 6/' .planning/ROADMAP.md | grep -c '^- \[x\]'</automated>
  </verify>
  <acceptance_criteria>
    - All Phase-5 checkboxes ticked
    - "✅ Complete" status line added
    - Phase 6/7/8 checkboxes untouched
  </acceptance_criteria>
  <done>ROADMAP reflects Phase 5 complete.</done>
</task>

<task type="auto">
  <name>Task 4: Add AOS Portfolio Autonomy Contract to STRUCTURE.md</name>
  <files>STRUCTURE.md</files>
  <read_first>
    - STRUCTURE.md (full or grep for "AOS Bootstrap Autonomy Contract" — the Phase 4
      contract section is the template). If STRUCTURE.md does not exist at repo
      root, check for a vault equivalent at `~/vaults/ark/STRUCTURE.md` per the
      Phase 4 plan's flexible path note.
  </read_first>
  <action>
    Append a new section after the Phase 4 contract (or wherever the contract
    series lives):

    ```markdown
    ## AOS Portfolio Autonomy Contract (Phase 5)

    **Entry point:** `ark deliver` with no args from outside a project directory.

    **Engine:** `scripts/ark-portfolio-decide.sh` — sourced by `ark-deliver.sh`.

    **Discovery:** projects = directories with `.planning/STATE.md` under
    `${ARK_PORTFOLIO_ROOT:-~/code}` (max depth 3).

    **Customer attribution:** `<project>/.planning/policy.yml::bootstrap.customer`;
    missing → `scratch` bucket (deprioritized but eligible).

    **Priority formula:**
    ```
    priority = stuckness * 3
             + falling_health * 2
             + (monthly_headroom > 20 ? 1 : 0)
             + ceo_priority * 5
    ```

    Signal sources:
    - `stuckness` (0|1|2): `status: blocked` in STATE.md = 2; mtime > 7d = 1; else 0.
    - `falling_health` (0|1): pass-count regression in newest delivery log.
    - `monthly_headroom` (0..100): `100 - (used / cap * 100)`; ≥ 80% used → 0 (DEFERRED_BUDGET).
    - `ceo_priority` (0|1): basename match against `## Next Priority` in
      `${ARK_PROGRAMME_MD:-~/vaults/StrategixMSPDocs/programme.md}`.

    **Tie-break:** highest mtime of `.planning/STATE.md` wins (most-recently-touched).

    **Decision classes (audit, single-writer `_policy_log "portfolio"`):**
    1. `SELECTED` — winner picked. context_json: full priority breakdown.
    2. `DEFERRED_BUDGET` — customer ≥ 80% monthly cap. Project skipped.
    3. `DEFERRED_HEALTHY` — top non-budget-deferred candidate has no actionable signals.
    4. `NO_CANDIDATE_AVAILABLE` — no projects under root, or all filtered.

    **Cool-down:** project DEFERRED in last 24h for the same reason class is
    skipped (read-only sqlite SELECT against `~/vaults/ark/observability/policy.db`).

    **Backward compat:** `ark deliver --phase N` and `ark deliver` from inside a
    project (`.planning/ROADMAP.md` present) bypass the portfolio engine entirely.

    **Requirements:** REQ-AOS-23..30. **Verify suite:** Tier 11.
    ```

    Atomic write.
  </action>
  <verify>
    <automated>grep -q 'AOS Portfolio Autonomy Contract' STRUCTURE.md 2>/dev/null || grep -q 'AOS Portfolio Autonomy Contract' ~/vaults/ark/STRUCTURE.md 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Section "AOS Portfolio Autonomy Contract (Phase 5)" present in STRUCTURE.md
    - All 4 decision-class names appear
    - Priority formula present verbatim
  </acceptance_criteria>
  <done>Contract documented for future phases to consume.</done>
</task>

<task type="auto">
  <name>Task 5: Best-effort SKILL.md refresh</name>
  <files>SKILL.md</files>
  <read_first>
    - SKILL.md if it exists at repo root or `~/vaults/ark/SKILL.md`. If neither
      exists, this task is a no-op (best-effort).
  </read_first>
  <action>
    If SKILL.md exists, append a one-line capability under the Phase 4 entry:
    `- **Phase 5 (Portfolio Autonomy):** \`ark deliver\` from outside any project routes via portfolio priority engine; per-customer budget caps; CEO directive override; 24h cool-down.`

    If SKILL.md does not exist, skip and note in 05-07-SUMMARY.md.
  </action>
  <verify>
    <automated>grep -q 'Phase 5 (Portfolio Autonomy)' SKILL.md 2>/dev/null || grep -q 'Phase 5 (Portfolio Autonomy)' ~/vaults/ark/SKILL.md 2>/dev/null || echo "SKILL.md absent — best-effort skipped"</automated>
  </verify>
  <acceptance_criteria>
    - If SKILL.md exists: line added
    - If absent: SUMMARY notes skip
  </acceptance_criteria>
  <done>Skill surface refreshed (best-effort).</done>
</task>

</tasks>

<verification>
- 8 REQ-AOS-23..30 rows present in REQUIREMENTS.md
- STATE.md frontmatter `completed_phases: 5`
- ROADMAP.md Phase 5 fully ticked
- STRUCTURE.md has new contract section
- SKILL.md updated or skip noted
</verification>

<success_criteria>
All Phase 5 documentation truth-up complete. Phase 5 closed.
</success_criteria>

<output>
After completion, create `.planning/phases/05-portfolio-autonomy/05-07-SUMMARY.md`
listing each updated file + key change + Tier numbers retained.
</output>
